#include <stdlib.h>
#include <stdio.h>
void insertion_sort(int *a, int n) {
    int x=0, y=0;
    int key=0; 
    for(x=1; x<n; x++) {
        key = a[x];
        y = x-1;
        while(y>=0 && a[y]>key) {
            a[y+1] = a[y];
            y = y-1;
        }
        a[y+1] = key;
    }
    
}
